<?xml version="1.0" encoding="utf-8"?>
<!--
  Deliberately minimal scope: only typeWindowStateChanged (foreground app
  changes), canRetrieveWindowContent="false" (never reads on-screen content,
  only which package is in the foreground). packageNames is intentionally
  omitted — the actual blocklist is user-configured and dynamic, so this
  service must be able to observe every app's foreground events and match
  against BlockingPrefs' current list at runtime, not a static set declared
  here.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:notificationTimeout="100"
    android:canRetrieveWindowContent="false"
    android:description="@string/accessibility_service_description" />
